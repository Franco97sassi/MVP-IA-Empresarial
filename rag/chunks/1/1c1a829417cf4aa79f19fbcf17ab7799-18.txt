bablemente recuperes poco o nada, dependiendo del embedding. Esto valida el ajuste de umbrales de similitud. El umbral se toma desde RagSearchOptions.MinSimilarityScore o desde configuración si no se manda. 11. Checklist de aceptación de Etapa 7 Usá esta lista para confirmar que está todo OK. Backend ● Puedo subir .md, .txt o .pdf. ● Se crean chunks. ● Se generan embeddings sin error. ● GET /api/documents devuelve documentos con chunkCount. ● GET /api/documents/{id}/chunks devuelve chunks. ● POST /api/chat/send con pregunta documental devuelve usedRag: true. ● La respuesta trae sources. ● Cada fuente trae score. ● Cada fuente trae vectorScore. ● Cada fuente trae keywordScore. ● Cada fuente trae rankScore. ● Cada fuente trae preview. ● El filtro documentIds limita las fuentes. ● POST /api/documents/rag/evaluate devuelve métricas de evaluación.