# Seguridad del sistema

El sistema protege rutas con JWT. También valida mensajes para evitar intentos
de prompt injection. Las métricas registran tiempo de respuesta, ruta usada,
uso de herramientas y cantidad de chunks RAG utilizados.